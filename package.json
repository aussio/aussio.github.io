{
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel index.html --open",
        "build": "rm -rf dist && parcel build index.html",
        "clean": "rm -rf ./node_modules; rm -rf ./.parcel-cache",
        "predeploy": "rm -rf dist && parcel build index.html --public-url https://aussio.github.io/",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "pixi.js": "8.1.0"
    },
    "devDependencies": {
        "gh-pages": "latest",
        "parcel": "latest"
    }
}